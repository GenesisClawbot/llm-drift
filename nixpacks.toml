# Railway Nixpacks configuration
# Used if Railway opts for Nixpacks instead of Dockerfile

[phases.install]
cmds = ["pip install -r requirements-backend.txt"]

[start]
cmd = "python3 -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
